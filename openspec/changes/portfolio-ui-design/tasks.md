## 1. Design System & Global Styles

- [x] 1.1 Refine Tailwind CSS theme tokens and base styles in `src/app/globals.css` and verify styling variables load correctly
- [x] 1.2 Document visual system direction in `DESIGN.md` and verify design principles alignment

## 2. Layout & Navigation Components

- [x] 2.1 Implement `Navbar` component with brand logo, smooth section links, and quick CV dropdown, and verify responsive behavior
- [x] 2.2 Implement `Footer` component with social links, copyright, and back-to-top trigger, and verify links open safely in new tabs

## 3. Core Content Sections

- [x] 3.1 Implement `Hero` section with status pill, headline, summary bio, and primary CTA buttons (CV download, contact) and verify button interactions
- [x] 3.2 Implement `ProjectCard` and `ProjectsSection` with screenshot fallback, tech badges, and GitHub/Demo links, and verify grid layout across breakpoints
- [x] 3.3 Implement `ExperienceSection` displaying the career timeline and achievement bullets, and verify typography and date alignments
- [x] 3.4 Implement `SkillsSection` displaying categorized interactive skill chips, and verify category grouping and contrast

## 4. Integration & Polish

- [x] 4.1 Assemble all components in `src/app/page.tsx` and verify seamless visual flow and layout integrity
- [x] 4.2 Validate build with `npm run build` and run Impeccable detector to verify zero regressions, full responsive support, and WCAG AA contrast
