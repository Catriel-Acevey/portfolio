# Proposal: Configure Static Export and Deploy to Firebase

Configure Next.js for static export (`output: "export"`) and update Firebase Hosting to serve the `out` directory to ensure the latest version of the portfolio is deployed.
