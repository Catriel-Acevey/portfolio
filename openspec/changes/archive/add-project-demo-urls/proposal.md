# Proposal: Add Deployment Links for Weather App and TGIF

Add official deployment URLs (`demoUrl`) and repository URLs (`githubUrl`) for Weather App (`https://catriel-weather.web.app/`) and TGIF (`https://tgif-page.web.app/`) in `portfolioData.ts`.
